spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciamentodetarefa
    username: postgres
    password: comi1bolo
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
Springdoc:
  openapi:
    title: 'Gerenciamento de Tarefas'
    description: 'API para gerenciamento de tarefas'
    version: '1.0.0'
