spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciamentodetarefa?serverTimezone=UTC
    username: postgres
    password: comi1bolo
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
Springdoc:
  openapi:
    title: 'Gerenciamento de Tarefas'
    description: 'API para gerenciamento de tarefas'
    version: '1.0.0'
